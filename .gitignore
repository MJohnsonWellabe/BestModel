# Licensed S&P Capital IQ / SNL data — never commit raw pulls
data/raw/**
!data/raw/.gitkeep

# Build artifacts derived from licensed raw data (regenerate via: python src/build.py).
# The tool ships with embedded DEMO data and loads this locally when present.
tool/data.json
data/processed/missingness.csv

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Curated PUBLIC subset (bluebook-derivable) — committed on purpose so the site auto-loads it.
!tool/public_data.json
